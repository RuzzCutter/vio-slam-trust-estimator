# План эксперимента (актуальная версия)

> Журнал выполнения: [`журнал_эксперимента.md`](журнал_эксперимента.md)  
> Инструкция по утилитам: [`README.md`](README.md)  
> Текст статьи: [`Статья_v2.txt`](Статья_v2.txt)  
> Последнее обновление: 2026-06-18

> **Формулы:** ниже используются ASCII-блоки и `моноширинные` имена переменных — они корректно отображаются в обычном Markdown preview без LaTeX. Для красивого рендера формул можно открыть файл с расширением *Markdown Math* или экспортировать в PDF через Pandoc.

## Текущий статус

| Этап | Статус |
|------|--------|
| 1. Окружение и сборка OpenVINS | **готово** |
| 2. Тестовые запуски без датасетов | **готово** |
| 3. trust_estimator + адаптивная ковариация | **готово** (smoke + MH_04 clean) |
| 4. Карта достоверности (раздел 4.3–4.4 статьи) | следующий |
| 5. Прогоны на датасетах + деградация | **готово** (ручные + batch) |
| 6. Таблицы и текст статьи | **в работе** |

---

## Гипотеза

Адаптивная оценка достоверности визуальных измерений `c(t)` снижает навигационную ошибку и повышает устойчивость локализации по сравнению с классическим VIO при **деградации** визуального потока. На **чистых** данных adaptive не должен ухудшать точность:

```text
ATE_adaptive  ≈  ATE_baseline   (допуск: ATE_adaptive <= 1.05 * ATE_baseline)
```

---

## Принятые решения

1. **Сравнение:** только OpenVINS baseline vs OpenVINS+adaptive (VINS-Mono/ORB-SLAM3 — литобзор).
2. **Карта достоверности:** входит в первую публикацию, реализуется после адаптивной ковариации.
3. **Данные:** EuRoC + TUM-VI; натурные эксперименты — только описание в тексте.
4. **Диск:** ~57 ГБ свободно; minimal EuRoC скачан (MH_01, MH_04, MH_05).
5. **Срок:** упрощать протокол; часть результатов допустимо экстраполировать.

---

## Математическое ядро эксперимента

### 1. Состояние и фильтр (OpenVINS MSCKF)

В каждый момент времени `t` фильтр оценивает состояние `x_t` (поза IMU, скорость, смещения, клоны поз в скользящем окне, параметры SLAM-точек). Прогноз — по IMU; визуальные измерения MSCKF вносят коррекцию через EKF-update.

| Режим | Шум измерений |
|-------|----------------|
| **Baseline** | фиксированный `R0` |
| **Adaptive** | масштабируется через `c(t)` |

---

### 2. Визуальное измерение (упрощённая модель)

Для 3D-точки `i` и камеры `c`:

```text
z[i,c] = h(x, f[i]) + v[i,c]

v[i,c] ~ N(0, sigma_pix^2 * I_2)
```

| Символ | Смысл |
|--------|--------|
| `z[i,c]` | измеренные пиксельные координаты (R^2) |
| `h(.)` | модель проекции (калибровка + поза камеры) |
| `f[i]` | параметры i-й 3D-точки |
| `sigma_pix` | базовый шум пикселя (`up_msckf_sigma_px`, по умолчанию 1 px) |
| `sigma_pix_0^2` | опорная дисперсия в baseline |

Невязка (residual) для inlier-набора:

```text
r = z - h(x_hat)     // r in R^(2N)
H = d h / d x_aug    // якобиан
```

---

### 3. Baseline: фиксированная ковариация

```text
R0 = sigma_pix_0^2 * I_(2N)

Omega_0 = R0^(-1) = sigma_pix_0^(-2) * I     // постоянная информационная матрица
```

---

### 4. Признаки достоверности f1 … f4

На каждом MSCKF-update: `f_k in [0, 1]` (`TrustEstimator::compute`).

#### f1 — доля inlier-ов (геометрия трекинга)

```text
f1 = N_inliers / N_features
```

| Переменная | Описание |
|------------|----------|
| `N_features` | число отслеживаемых особых точек |
| `N_inliers` | число точек после gating (chi2-тест OpenVINS) |

При деградации изображения: `f1` падает.

#### f2 — репроекционная ошибка

```text
e_repr_mean = (1 / N_inliers) * sum_i ||r_i||_2     // по inlier-ам

f2 = exp(-e_repr_mean / 2)
```

| Переменная | Описание |
|------------|----------|
| `e_repr_mean` | средняя L2-норма пиксельной невязки |
| делитель `2` | эмпирический масштаб (px), зашит в код |

Большие невязки -> `f2 -> 0`.

#### f3 — пространственное покрытие изображения

Сетка `G x G` (`trust_grid_size`, по умолчанию G=8):

```text
f3 = (число заполненных ячеек) / G^2

ячейка (u,v) заполнена, если в ней есть >= 1 inlier
```

Координаты нормируются на размер кадра (752 x 480 для EuRoC). Кластеризация точек -> `f3` падает.

#### f4 — неопределённость позы (вероятностный признак)

```text
f4 = exp(-tr(P_pos) / tau)

tr(P_pos) = P_xx + P_yy + P_zz     // только блок позиции 3x3
```

| Переменная | Описание |
|------------|----------|
| `P_pos` | 3x3 блок позиции из ковариации клона IMU-позы |
| `tau` | `trust_tau`, по умолчанию 5.0 |

> **Важно:** не использовать полный trace 6x6 — дисперсия ориентации обнуляет f4 на длинных траекториях (fix 2026-06-18).

---

### 5. Интегральный коэффициент доверия c(t)

```text
c(t) = clip( (sum of active f_k) / n_active ,  c_min,  c_max )

n_active = 4 (full) или 3 (ablation: один f_k отключён через trust_use_f*)
c_min = 0.05 ,  c_max = 1.0     // зашиты в код
```

`c(t)` — скалярная оценка качества визуального update в момент `t`.

---

### 6. Адаптивная ковариация измерений

```text
s(t) = min( 1 / (c(t) + eps) ,  s_max )

sigma_pix_eff^2(t) = sigma_pix_0^2 * s(t)
                   = sigma_pix_0^2 / (c(t) + eps)     // с ограничением s_max

R_t = sigma_pix_eff^2(t) * I_(2N)
```

| Параметр | YAML-ключ | По умолчанию |
|----------|-----------|--------------|
| `eps` | `trust_eps` | 0.05 |
| `s_max` | `trust_r_max_multiplier` | 100 |

**Смысл:** низкий `c(t)` -> больший шум -> меньший вес vision в EKF -> фильтр сильнее опирается на IMU.

Информационная формулировка (статья, п. 4.1):

```text
Omega_t = R_t^(-1)  ~  (c(t) + eps) / sigma_pix_0^2   ~  c(t)
```

---

### 7. Отказ от визуального update (п. 4.2)

```text
если  c(t) < c_skip  ->  пропустить EKFUpdate (только IMU)

c_skip = 0.15   (trust_skip_threshold)
```

---

### 8. EKF-update (MSCKF)

```text
S = H * P * H^T + R_t
K = P * H^T * S^(-1)

x_hat+ = x_hat- + K * r
P+     = (I - K * H) * P-
```

| Символ | Описание |
|--------|----------|
| `P` | априорная ковариация |
| `H` | якобиан stacked residuals |
| `r` | вектор невязок |
| `R_t` | adaptive или фиксированный `R0` |

**Код:** `UpdaterMSCKF.cpp` — `sigma_pix_sq_effective` перед `R_big`.

---

### 9. Метрики эксперимента

#### ATE (Absolute Trajectory Error)

После Sim(3)-выравнивания (`evo --align --correct_scale`):

```text
ATE_RMS = sqrt( (1/N) * sum_k || p_k_est - p_k_gt ||_2^2 )
```

| Символ | Описание |
|--------|----------|
| `p_k_est` | оценённая позиция (`trajectory_tum.txt`) |
| `p_k_gt` | ground truth (MH_04, MH_05) |
| `N` | число синхронизированных пар |

#### RPE (Relative Pose Error)

```text
RPE_RMS = sqrt( (1/M) * sum_j || delta_p_j ||_2^2 )     // delta = 1 m
```

#### Дополнительные метрики

| Метрика | Определение |
|---------|-------------|
| Tracking failure | разрыв > 0.5 с, `[init]: failed`, или `diverged=1` (max\|coord\| > 50 m) |
| `c_mean` | среднее `c(t)` по `trust_log.csv` |
| CPU/RAM | `/usr/bin/time -v` |

---

### 10. Модели деградации (п. 5.3 статьи)

Только изображения в bag; IMU не меняется.

#### D1 — Gaussian blur

```text
I'(x,y) = (G_sigma * I)(x,y)
```

| Параметр demo | sigma |
|---------------|-------|
| `gaussian:5` | 5 px |
| `gaussian:9` | 9 px |

#### D3 — засветка

```text
I'(x,y) = clip( I(x,y) * (1 + p/100),  0, 255 )
```

| Параметр demo | p |
|---------------|---|
| `brightness:40` | +40% |
| `brightness:60` | +60% |

**Ожидаемая цепочка:** sigma или p растёт -> f1,f2,f3 падают -> c(t) падает -> adaptive ослабляет vision.

---

### 11. Критерии подтверждения гипотезы

```text
1. Деградация:  ATE_adaptive < 0.9 * ATE_baseline   (>= 2 сценария)
2. Failures:    N_fail_adaptive <= N_fail_baseline
3. Чистые:      ATE_adaptive <= 1.05 * ATE_baseline
4. Монотонность: c_mean(D[i+1]) < c_mean(D[i]) при усилении деградации
```

---

### 12. Карта достоверности (задел, п. 4.3–4.4)

Не реализовано. План:

```text
C(p) = sum_t  w_t * c(t)  /  sum_t w_t

w_t = exp( -|| p_hat_t - p ||^2 / (2*r^2) )     // только точки в радиусе r
```

Упрощение: 2D-сетка в плоскости xy; штраф в планировщике при `C(p) < c_th`.

---

### 13. Лог trust_estimator (`trust_log.csv`)

| Колонка | Символ |
|---------|--------|
| `timestamp` | t |
| `f1` … `f4` | признаки |
| `c` | c(t) |
| `n_features`, `n_inliers` | N_features, N_inliers |
| `e_repr` | e_repr_mean |
| `tr_pose` | tr(P_pos) |
| `skip_update` | 1 если update пропущен |
| `noise_scale` | s(t) |

**Код:** `ov_msckf/src/trust/TrustEstimator.{h,cpp}`, `UpdaterMSCKF.cpp`  
**Конфиг baseline:** `config/euroc_mav` (`trust_enable: false`)  
**Конфиг adaptive:** `config/euroc_mav_trust` (`trust_enable: true`)

---

## Архитектура (два подэтапа)

### 2a. Адаптивная ковариация (реализовано)

```text
Датасет -> OpenVINS baseline:  R_t = R0 = sigma_pix_0^2 * I
        -> OpenVINS adaptive:   f1..f4 -> c(t) -> R_t = s(t) * R0
```

### 2b. Карта достоверности (следующий шаг)

```text
c(t) + поза -> накопление в сетке -> карта C(x,y,z) -> штраф в планировщике
```

---

## 1. Окружение

| Компонент | Версия | Статус |
|-----------|--------|--------|
| Ubuntu | 24.04 LTS | OK |
| ROS 2 | Jazzy (`/opt/ros/jazzy`) | OK |
| OpenCV, Eigen, Boost, Ceres | apt | OK |
| OpenVINS | `aspiranture/ws_vins` | **собран** |
| Python venv | `aspiranture/venv` | OK (evo, opencv, pandas, …) |
| Патч Jazzy-заголовков | `ov_msckf/src/ros/ROS2*.h` | применён |

### Сборка

```bash
source /opt/ros/jazzy/setup.bash
cd /home/g-tancyura/aspiranture/ws_vins
colcon build --symlink-install --cmake-args -DCMAKE_BUILD_TYPE=RelWithDebInfo
source install/setup.bash
```

После изменений trust_estimator:

```bash
colcon build --packages-select ov_msckf --symlink-install
```

### Smoke-тест

```bash
bash scripts/test_smoke.sh
bash scripts/test_tools.sh
```

### Прогон EuRoC

Скрипт: `scripts/run_euroc.sh`  
Launch: `launch/euroc_baseline.launch.py`, `launch/euroc_adaptive.launch.py`

| Режим | Команда | Когда |
|-------|---------|-------|
| Headless | `bash scripts/run_euroc.sh baseline MH_01_easy --duration 60` | smoke, batch |
| Полный bag | `bash scripts/run_euroc.sh baseline MH_04_difficult --eval` | ATE |
| RViz | `bash scripts/run_euroc.sh adaptive MH_01_easy --viz` | demo |
| Меню | `python3 scripts/run_demo.py` | compare, деградация |

#### Start-offset

Пропускает начало bag (подъём/переноска дрона). Значения — в **`config/datasets.yaml`** (читаются через `datasets_registry.py` → `euroc_common.sh`). **Не сдвигает GT** — evo выравнивает по timestamp.

| Последовательность | Offset |
|--------------------|--------|
| MH_01_easy | 40 с |
| MH_02_easy | 35 с |
| MH_04_difficult | 15 с |
| MH_05_difficult | 5 с |
| MH_03_medium | 5 с |

Переопределение: `--start-offset N` или меню demo «Расширенные параметры».

#### Расширенные параметры (demo / CLI)

| Параметр | CLI | Назначение |
|----------|-----|------------|
| start-offset | `--start-offset` | начало воспроизведения bag |
| retries | `--retries` | повторы при расхождении (default 3) |
| trust_tau | `--trust-tau` | τ в формуле f₄ (adaptive) |
| ablate | `--ablate f1` | ablation: отключить один признак (f1…f4) |

```bash
bash scripts/run_euroc.sh adaptive MH_04_difficult --ablate f1 --degrade gaussian:5 --eval
python3 scripts/run_batch.py --plan config/batch_ablation.yaml
python3 scripts/build_tables.py --batch datasets/results/batches/ablation_d1_... --kind ablation
```

**Длина пути:** `path_m` — сумма длин шагов odometry (~90 m для MH_04, как GT).  
`span_m` — расстояние start→end (для петли может быть << path_m). Это не ошибка размерности.

#### Сравнение baseline vs adaptive

```bash
python3 scripts/run_demo.py --mode compare --seq MH_04_difficult --eval
python3 scripts/run_demo.py --mode compare --seq MH_04_difficult --degrade gaussian:5 --eval
```

- **Compare** — оба прогона headless (честное ATE).
- **MH_01** — smoke, GT для ATE нет.
- **MH_04 / MH_05** — основные последовательности для статьи.

Результаты: `datasets/results/<SEQ>_<mode>_<timestamp>/`

---

## Датасеты

- **Каталог:** `config/datasets.yaml` — все MH_01…05, offset, GT, источники загрузки
- **Custom:** `config/datasets_custom.yaml` (шаблон `.example`) — локальный bag или URL
- **Minimal bundle:** MH_01, MH_04, MH_05 · **Full:** + MH_02, MH_03
- Конфиги OpenVINS: `euroc_mav`, `euroc_mav_trust`

```bash
python3 scripts/lib/datasets_registry.py list
bash scripts/download_datasets.sh --seq MH_04_difficult --ros2
python3 scripts/run_demo.py --datasets
```

### Ground truth для ATE

| MH | Bag (EuRoC) | GT в `ov_data/euroc_mav/` |
|----|-------------|---------------------------|
| MH_01…03 | да | **нет** (OpenVINS не положил txt) |
| MH_04, MH_05 | да | **да** |
| V1_*, V2_* | отдельно | да (файлы есть, bag не в minimal) |

Для ATE на MH_02 нужно сконвертировать EuRoC CSV → TUM и указать `gt_file` в custom YAML.

Источник bag: [ETH Research Collection](https://doi.org/10.3929/ethz-b-000690084)

---

## Деградация

| ID | Тип | Параметр demo |
|----|-----|---------------|
| D1 | Gaussian blur | `gaussian:5`, `gaussian:9` |
| D3 | Засветка | `brightness:40`, `brightness:60` |

Скрипты: `degrade_ros2_bag.py` (bag), `degrade_dataset.py` (offline, задел).  
Кэш: `datasets/euroc/degraded/<SEQ>_<type><level>_ros2/`

---

## Таблицы для статьи

Скрипты: `run_batch.py` (пакетные compare), `build_tables.py` (md / LaTeX / csv).

```bash
python3 scripts/run_batch.py --plan config/batch_paper.yaml
python3 scripts/build_tables.py --batch datasets/results/batches/paper_main_... --out paper_tables/
```

План по умолчанию (`config/batch_paper.yaml`): MH_04/05 × clean + D1 (σ=5,9) + D3 (+40%, +60%).

Содержание таблиц:

1. ATE/RPE на чистых EuRoC (MH_04, MH_05)
2. ATE vs D1/D3
3. Tracking failures (колонка Fail в `table_all.md`)
4. Ablation f1 … f4 — `config/batch_ablation.yaml`, таблица `table_ablation.md`
5. (опционально) карта достоверности vs ATE на участках траектории

---

## Известные проблемы

| Проблема | Решение |
|----------|---------|
| Jazzy headers | патч `__has_include` в ROS2Visualizer.h |
| f4 -> 0, расходимость | только tr(P_pos), не 6x6 |
| MH_01: нет ATE | GT только MH_04/05 |
| Adaptive лучше baseline на clean | проверить tuning; гипотеза — на деградации |

---

## Файлы проекта

```text
aspiranture/
├── README.md
├── config/
│   ├── datasets.yaml           <- реестр последовательностей
│   ├── batch_paper.yaml        <- план пакетных прогонов
│   ├── batch_ablation.yaml     <- ablation f₁…f₄
│   └── datasets_custom.yaml.example
├── paper_tables/               <- выход build_tables.py
├── datasets/results/batches/   <- manifest batch-прогонов
├── scripts/run_batch.py
├── scripts/build_tables.py
├── scripts/lib/run_results.py
├── эксперимент_v2.md
├── журнал_эксперимента.md
├── launch/euroc_*.launch.py
├── datasets/euroc/
├── datasets/euroc/degraded/
├── datasets/results/
├── scripts/run_demo.py
├── scripts/run_euroc.sh
├── scripts/lib/datasets_registry.py
└── ws_vins/
```
